# 保险合规审核系统：基于新增合规知识与本次评估结果的升级方案

## 1. 结论先行

当前项目的主架构方向仍然成立：
- 保留 `Stage 0-3` 的多阶段流水线是对的；
- 保留 `base + skill` 双轨也是对的；
- 保留 `AC + 规则 + LLM` 的混合形态也是对的。

但结合 `plan/合规知识-外发.txt` 与本次 `smoke` 评估结果，当前系统还没有把“审查点知识”真正变成一层可执行约束。

现阶段最值得做的，不是重构主框架，也不是上更重的知识图谱或平台，而是做一轮 **轻量的、知识驱动的升级**：

1. 把合规知识从“参考文档”升级为“结构化审查点层”；
2. 把模型判断从“泛化语义判定”收缩为“场景识别 + 审查点核验 + 例外校验”；
3. 把当前评估暴露出的高频误报/漏报场景，优先固化成 deterministic gate 与 override。

一句话判断：
**这个项目现在的瓶颈不是模型不够大，而是审查点知识还没有充分进入召回、路由、判定、纠偏四个关键环节。**

---

## 2. 我对项目背景、难点和需求的理解

### 2.1 项目目标

这是一个保险营销文本合规审核系统，目标不是做一般文本分类，而是做：
- 保险场景下的合规违规识别；
- 精确到审查点级别的判断；
- 尽可能稳定的误报控制；
- 字符级定位与可解释输出；
- 在 SLA 约束下可运行。

### 2.2 真正的难点

这个问题最难的地方不是“识别关键词”，而是以下几点同时成立：

1. **同词不同义**
   - “国家级”“最高”“理财”“顾问”“减保”“监管要求”等词，是否违规，强依赖上下文。

2. **很多审查点是条件成立型，而不是词命中型**
   - 例如：
     - 绝对化表述，未必都违规；
     - 减保/保单贷款，未必都违规；
     - 监管、法律、税务内容，只有在“背书/夸大/误导”时才违规。

3. **很多审查点存在强例外条件**
   - 新增知识文件已经非常明确：
     - 存在提示语时，风险可能下降；
     - 非保险产品/服务/代理人语境下，绝对化风险可能较低；
     - 客观说明监管或产品机制，不等于可以用于营销背书。

4. **很多误报来自“营销相关词”与“合规违规点”混淆**
   - 项目当前会把一些“保险语境”直接近似映射为“违规语境”；
   - 但合规审查真正要识别的是“违规表达方式”，不是“出现保险词就可疑”。

5. **很多漏报来自审查点没有被单独建模**
   - 如：
     - 合同外利益；
     - 监管背书；
     - 招募代理人误导头衔；
     - 避债/避税/税法误导；
     - 传承/资产转移类误导。

---

## 3. 本次评估结果说明了什么

### 3.1 基线结果

本次 `smoke` 评估官方汇总位于：
- `benchmark/reports/smoke_run_20260313/summary.json`
- `benchmark/reports/smoke_run_20260313/summary.md`

汇总结果：
- 总样本：`28`
- Precision：`0.4`
- Recall：`0.375`
- F1：`0.3871`
- 类别命中率：`0.4643`
- 定位命中率：`0.1429`

补充说明：
- `raw_results.jsonl` 在重复跑批过程中出现了部分重复样本写入；
- 对原始结果按 `sample_id` 去重后复盘，结论方向没有变，只是误报指标略优于官方快照；
- 因此，下面的升级建议主要依据 **稳定问题方向**，而不是依赖单一数字的细小波动。

### 3.2 稳定暴露出来的三个问题

#### 问题 A：误报仍然集中在“语义像风险，但不构成违规”的场景

本轮典型误报主要集中在：
- `badcase_0023`
- `badcase_0026`
- `badcase_0030`

对应现象：
- 提到了保险、理赔、理财、监管、条款等词；
- 但并没有真正形成违规宣传；
- 模型或路由仍容易把“保险语境”误判成“违规语境”。

说明：
**当前系统缺少“先判断是不是审查点语境，再判断是否违规”的前置闸门。**

#### 问题 B：漏报集中在“强监管审查点，但当前没有单独建模”的类别

去重复盘后，漏报最集中在以下类别：
- `gifts_or_extra_benefits`
- `agent_title_or_recruitment`
- `national_or_regulatory_endorsement`
- `tax_or_law_misinterpretation`
- `transfer_or_inheritance`
- `guaranteed_return`
- `comparison_or_absolute`

这与新增知识文件高度一致。

说明：
**当前知识库虽然有规则卡，但对这些“监管型审查点”还没有形成足够强的场景锚点、例外条件和专门判定模板。**

#### 问题 C：判对了也不一定判准，定位与类别归因都偏弱

本轮还有两类明显问题：
- `comparison_or_absolute` 与 `financial_product_confusion` 之间互相串类；
- 定位命中率仅 `0.1429`，说明模型能判断风险，但没有稳定对准触发证据。

说明：
**系统当前更像“做了一个结果”，而不是“锁定了哪个审查点、哪句触发、为什么触发”。**

---

## 4. 新增合规知识文件带来的关键启发

`plan/合规知识-外发.txt` 的价值，不在于它更长，而在于它把审查逻辑说得更像“规则裁判标准”。

其中最重要的启发有四个：

### 4.1 审查点必须按“监管问题类型”组织，而不是按通用 NLP 类别组织

例如当前项目里的 `financial_product_confusion`、`comparison_or_absolute` 仍偏抽象；
但知识文件里的审查点更接近真实审核标准：
- 简单对比；
- 绝对化用语；
- 监管背书；
- 避债/避税/遗产税误导；
- 保单贷款误导；
- 减保退保误导；
- 给予保险合同外利益；
- 招募代理人误导头衔。

这类结构更适合作为系统内部的“第一层语义单位”。

### 4.2 很多审查点都带有“例外”和“提示语”要求

例如：
- 绝对化表述：若未指向产品、服务、代理人，风险可能较低；
- 减保：介绍功能本身未必违规，但不能作为亮点，且需要提示不利后果；
- 保单贷款：可以介绍应急属性，但不能宣传成灵活理财工具；
- 监管：可客观描述制度要求，但不能做增信背书。

这意味着：
**模型不能只做“找风险词”，必须同步做“找缓释条件/提示语/适用边界”。**

### 4.3 一些场景先天适合 deterministic 处理，而不是继续加大模型负担

例如：
- “减保会降低保障额度及现金价值，请谨慎选择” 这类提示语；
- “保险产品组合/保险产品计划” 这类规范标签；
- “理财/存款/返本/储蓄/账户/存折”等混淆词；
- “国家级/最高级/最佳/唯一”等绝对化词；
- “税务规划师/金融理财顾问/合伙人”等招募误导头衔。

这些都更适合先做结构化锚点，再决定是否交给 LLM。

### 4.4 审查的核心不是“文本理解”，而是“判断是否踩到了可证明的审查点”

这意味着模型输出也要变：
- 不能只说 `violation/compliant`；
- 必须给出是哪个审查点、由什么锚点触发、有没有被例外条件覆盖。

---

## 5. 最值得做的升级：不是重构，而是补一层“审查点知识层”

这是我认为 ROI 最高的一项升级。

### 5.1 目标

在现有 `rule_cards` 之上，补一层轻量结构化审查点定义，不做重型知识图谱。

### 5.2 建议的最小结构

每个审查点至少补充这些字段：
- `audit_point_id`
- `audit_point_name`
- `risk_family`
- `applicable_scope`
- `risky_anchors`
- `mitigating_anchors`
- `required_disclaimers`
- `forbidden_claim_patterns`
- `allowed_neutral_patterns`
- `subject_scope`
- `preferred_route`
- `evidence_hint`

### 5.3 第一批优先结构化的审查点

建议先做下面这些，因为它们和本轮错例最吻合：
- 简单对比 / 绝对化表述
- 金融产品混淆表述
- 收益承诺 / 过往业绩暗示未来
- 监管背书 / 国家背书 / 政策背书
- 税务与法律误导（避税、避债、遗产税、税优误读）
- 保单贷款误导
- 减保退保误导
- 合同外利益（礼品、抽奖、卡券、红酒会等）
- 招募代理人误导头衔 / 用工性质模糊
- 传承/资产转移类误导

### 5.4 为什么这一步最重要

因为它会同时改善四个环节：
- Stage 1：召回更准；
- Stage 1.8：路由更稳；
- Stage 2：模型提示更聚焦；
- Stage 2.5：纠偏更有依据。

---

## 6. 具体升级方向（按优先级排序）

## 6.1 P0：把模型任务改成“审查点核验”，不要再让它做泛化裁判

### 目标

模型先回答这几个问题，再给结论：
1. 这段话是不是保险营销/产品宣传/增员招募语境？
2. 命中了哪个具体审查点？
3. 风险锚点是什么？
4. 是否存在缓释条件、例外、提示语？
5. 最终是违规、合规还是仅需人工复核？

### 需要模型新增的输出维度

建议至少包含：
- `audit_point_id`
- `subject_scope`
- `promotion_intent`
- `positive_anchors`
- `negative_anchors`
- `required_disclaimer_present`
- `decision_basis`
- `evidence_span_ids`

### 为什么必须做

因为本轮很多误报不是模型“理解错句子”，而是模型没有先做：
- 是否属于真正营销语境；
- 是否真的落在审查点定义内；
- 是否已有规范提示语。

---

## 6.2 P0：增加“前置场景闸门”，先过滤掉高频误报场景

### 目标

在进入重判断前，先做几个 deterministic scene gate。

### 优先加的闸门

#### 闸门 1：非产品/非营销上下文的绝对化表述

针对知识文件中“绝对化表述存在低风险例外”的要求，优先拦截：
- 纯历史人物/调侃/故事语境；
- 仅表达主观感受，不指向产品、服务、代理人、经营业绩；
- 仅作背景介绍，不构成营销主张。

解决的典型错例：
- `badcase_0003`
- `badcase_0004`

#### 闸门 2：规范提示语充分的减保/保单贷款说明

如果文本明确出现：
- 减保会降低保障额度；
- 减保会影响现金价值；
- 请根据实际需求谨慎选择；
- 保单贷款额度/期限/利息限制；

则不应轻易判违规，应降为 `compliant` 或 `review`。

解决的典型错例：
- `badcase_0011`

#### 闸门 3：中性知识说明 vs 销售话术

对于“介绍功能/说明规则/解释条款”的句子，若缺少：
- 诱导购买；
- 优势夸大；
- 对比贬损；
- 收益承诺；
- 背书增强；

则不应直接进高风险轨。

---

## 6.3 P0：为高漏报审查点补专门锚点与专门技能

### 当前最应该补强的 7 类

#### 1）合同外利益

知识文件已经明确：
- 礼品、奖品、卡券、抽奖、红酒会等都要谨慎；
- 即使不是直接送保单，也可能构成合同外利益。

本轮漏报样本：
- `违规样本3_0031`
- `违规样本3_0047`

建议：
- 单独做 `extra_benefit` 审查点；
- 强化锚点：`赠送`、`红酒会`、`抽奖`、`礼品`、`卡券`、`伴手礼`、`旅游`、`活动权益`。

#### 2）招募代理人误导头衔

知识文件非常明确：
- `金融理财顾问`、`康养顾问`、`税务规划师`、`合伙人` 等都属于高风险头衔。

本轮漏报样本：
- `违规样本3_0035`
- `违规样本3_0054`

建议：
- 单独做 `recruitment_title` 审查点；
- 不要把这类问题混在一般营销 skill 里。

#### 3）监管背书 / 国家背书

知识文件已经明确禁止：
- 用监管审批、监管要求、国家政策、政府联合推出等做产品增信背书。

本轮漏报样本：
- `违规样本3_0036`
- `违规样本3_0037`

建议：
- 单独做 `regulatory_endorsement` 审查点；
- 强化锚点：`监管要求`、`审批通过`、`备案`、`国家政策型`、`政府联合推出`、`国家背书`。

#### 4）税法/法律误导

知识文件明确强调：
- 避债、避税、遗产税、税务规划、债务规划等不能片面宣传。

本轮漏报样本：
- `违规样本3_0037`
- `违规样本3_0050`

建议：
- 将 `tax_or_law_misinterpretation` 细拆为：
  - 避税/免税误导；
  - 避债/债务规划误导；
  - 遗产税/财产归属误导；
  - 社保政策误导。

#### 5）传承/资产转移类误导

本轮漏报样本：
- `违规样本3_0005`

建议：
- 不要把“传承”仅视作一般销售术语；
- 应识别其是否在宣扬“有序转移”“规避争议”“财富隔离”等风险表达。

#### 6）收益承诺 / 稳定收益暗示

本轮漏报样本：
- `违规样本3_0032`

建议：
- 对“可能的、潜在的、稳定、锁定、提前规划收益”等组合表达做专门判断；
- 重点不是单词，而是“将不确定利益包装成可预期回报”的表达方式。

#### 7）简单对比 / 绝对化

本轮漏报样本：
- `违规样本2_0249`

说明：
- 目前对“越早投保越合算”“越早越便宜”这类相对绝对化表达仍不够敏感。

建议：
- 把“比较级/绝对判断 + 推销结论”作为单独 pattern；
- 不要只盯“最、第一、唯一”。

---

## 6.4 P1：把 Stage 2.5 从“反证校验”升级成“审查点 override 层”

当前 Stage 2.5 的方向是对的，但还可以更明确。

建议把它升级成：
- **不是只做否定纠偏**；
- 而是做“基于审查点知识的最终裁决修正”。

### 优先加入的 override 规则

1. **绝对化低风险例外 override**
   - 绝对化词未指向产品/服务/代理人/经营结果时，不应直接判违规。

2. **减保提示语 override**
   - 出现“降低保障额度/现金价值/谨慎选择”等提示时，优先降级风险。

3. **监管客观描述 override**
   - 仅客观说明制度要求、保险条款、法律效力，不等于监管背书。

4. **背景对比 override**
   - 如果只是背景性说明银行利率、经济环境，而不是导向产品优越性，则不宜直接判违规。

5. **非销售招募语境 override**
   - 如果只是泛称职业角色，而不是用于招募、岗位包装或收入诱导，不应直接命中招募类风险。

---

## 6.5 P1：升级类别体系，减少串类

本轮最明显的类别串扰是：
- `comparison_or_absolute` ↔ `financial_product_confusion`
- `guaranteed_return` → `financial_product_confusion`
- `responsibility_exaggeration` 判对了但无法归因

这说明当前类别仍过于宽。

建议做两件事：

### 1）把大类拆成“审查点子类”

例如：
- `financial_product_confusion`
  - 储蓄/存款/账户/存折混淆
  - 理财/投资计划混淆
  - 银行/基金/信托混淆
  - 保单贷款/现金价值概念混淆

- `comparison_or_absolute`
  - 绝对化用语
  - 简单收益对比
  - 简单金融产品对比
  - 贬低社保/他司产品

### 2）让模型输出“主审查点 + 次审查点”

这样可以减少“判成违规但归因不准”的情况。

---

## 6.6 P1：提升定位，不要只追求 verdict 正确

当前定位命中率偏低，核心原因不是定位算法弱，而是上游没有稳定锁定“真正触发的审查点证据”。

建议：
- 让模型优先指向“最小触发证据”，而不是泛指整个长句；
- 对“提示语”“风险语”“比较语”分别提 evidence；
- 对长文本中的多审查点，按审查点拆证据，不要合并成整段。

优先提升的不是字符算法，而是 **证据选择纪律**。

---

## 6.7 P2：评估模块也需要更贴近审查点

这不属于主链路改造，但对后续迭代非常重要。

建议：
- 评测样本按“审查点”分桶，而不是只按大类；
- `smoke` 集要单独保留一批 hard negative：
  - 非产品语境绝对化；
  - 合规提示语充分的减保/保单贷款；
  - 仅客观说明监管规则；
  - 仅背景介绍金融环境；
- 每次升级固定看 4 个指标：
  - Precision
  - Recall
  - badcase accuracy
  - location hit rate

另外，本次 `raw_results` 有重复写入现象，后续 benchmark 运行结果应强制按 `sample_id` 唯一化，避免污染评分结论。

---

## 7. 不建议现在做的事情

以下方向现在都不建议优先做：

### 7.1 不建议重做一套重型知识图谱或 RAG 平台

原因：
- 当前问题不是“知识找不到”，而是“知识没有被结构化进入判定链路”；
- 先做轻量审查点层，ROI 更高。

### 7.2 不建议单纯换更大模型来解决

原因：
- 本轮问题里大量是规则边界、例外条件、提示语判断；
- 这类问题仅靠更大模型很难稳定收敛，且成本更高。

### 7.3 不建议继续扩大粗召回而不加强负向闸门

原因：
- 当前误报已经说明，只加 recall 不加 gate，会把 `badcase` 压得更差。

### 7.4 不建议现在就做重平台的人审闭环系统

原因：
- 当前优先级仍是把单轮自动审核质量打稳；
- 人审平台可以后置。

---

## 8. 我建议的落地顺序

### 第一阶段：两周内可完成的轻量升级

目标：先把最明显错例打掉。

建议顺序：
1. 建立第一批审查点结构化定义；
2. 增加场景闸门：非营销绝对化、提示语充分的减保/保单贷款、客观监管说明；
3. 补强 7 个高漏报审查点的召回锚点与专用判断模板；
4. 收紧模型输出 schema，禁止自由发挥式 `decision_basis`。

### 第二阶段：稳定归因与定位

目标：从“判对”走向“判得清楚”。

建议顺序：
1. 主/次审查点输出；
2. 最小证据粒度约束；
3. override 层扩展为审查点裁决层；
4. `smoke + hard negative` 回归集固化。

### 第三阶段：再做更细的专项评测

目标：按监管审查点做持续优化。

建议分专项：
- 监管背书专项
- 招募代理人专项
- 合同外利益专项
- 保单贷款/减保专项
- 法税误导专项
- 金融产品混淆专项

---

## 9. 我对“最优方案”的判断

如果问题是：
**当前方案是不是最优？**

我的判断是：
- **架构层面，已经接近最优。**
- **知识落地层面，还没有到最优。**

更准确地说：
- 主框架不用推倒重来；
- 真正需要升级的是“知识组织方式”和“模型职责定义方式”。

因此，最优解不是重构，而是：
**在现有架构上，补一层审查点知识层，并以此重写召回、路由、判定、纠偏的约束方式。**

这条路最稳，也最符合当前项目的投入产出比。

---

## 10. 建议的验收目标

下一轮升级后，建议至少看这几个目标：

- `smoke` 去重视图下 Precision ≥ `0.80`
- Recall ≥ `0.55`
- `badcase` 准确率 ≥ `0.90`
- 定位命中率 ≥ `0.40`
- `监管背书 / 合同外利益 / 招募头衔 / 法税误导` 四类漏报明显下降
- 不再出现 schema 枚举不一致导致的结构化输出告警

---

## 11. 最终建议

当前最应该做的升级，不是“再加模型”，而是三件事：

1. **把审查点知识显式结构化**
2. **把模型任务收缩为审查点核验**
3. **把高频误报/漏报场景固化为 gate 与 override**

如果这三件事做完，项目质量会比继续堆 prompt、堆召回、堆模型更稳定。
