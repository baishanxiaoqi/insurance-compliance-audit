# 轻量升级方案审阅报告

日期：2026-03-12
审阅对象：`LIGHTWEIGHT_MODEL_UPGRADE_PLAN_20260312.md`

---

## 一、总体评价

这份升级方案是一份**非常务实、高质量**的技术方案文档。

### 优点

1. **目标明确**：不追求大而全，聚焦"降低误报、提高一致性、增强可控性"
2. **边界清晰**：明确列出"不做什么"，避免过度设计
3. **风险可控**：不重构主流水线，采用渐进式升级
4. **落地性强**：分 4 个 Phase 推进，每个阶段都有明确产出
5. **理念先进**：强调"升级模型的工作方式"而非"换更强的模型"

### 核心洞察

方案的核心洞察非常准确：

> **当前最该做的是：让规则更结构化，让模型更克制，让判断更可解释。**

这个判断完全符合当前项目的实际情况。

---

## 二、方案可行性分析

### ✅ 高度可行的升级项

#### 1. 升级项 1：规则卡片结构化（Phase 1-2）

**建议新增字段**：
- `actor_scope`：主体范围
- `claim_type`：主张类型
- `exception_group`：例外分组
- `evidence_required`：是否需要证据
- `route_hint`：路由提示
- `mutual_exclusion_group`：互斥分组

**可行性评估**：⭐⭐⭐⭐⭐
- 只需修改 `RuleCard` Schema
- 不影响现有流水线
- 可以逐步补充数据
- 立即可以开始实施

**预期收益**：
- Stage 1.8 路由更准确
- Stage 2 prompt 更清晰
- Stage 2.5 纠偏更容易

#### 2. 升级项 5：Prompt 改造（Phase 1）

**核心改进**：
- 从"宽泛判断"改成"约束式裁决"
- 明确 8 条工作准则
- 强化 `decision_basis` 输出

**可行性评估**：⭐⭐⭐⭐⭐
- 只需修改 Skills 的 system prompt
- 不改变系统架构
- 可以立即 A/B 测试
- 风险极低

**预期收益**：
- 误报率显著下降
- 判断更保守、更稳定
- 可解释性增强

#### 3. 升级项 2：Stage 1.5 锚点抽取（Phase 2）

**建议新增锚点**：
- `actor`：主体识别
- `claim`：主张类型
- `time_scope`：时间范围
- `evidence_need`：证据需求
- `tone_strength`：语气强度

**可行性评估**：⭐⭐⭐⭐
- Stage 1.5 已有基础（否定、数字、时态等）
- 可以基于规则和正则扩展
- 不需要引入重型 NLP 工具
- 可以逐步完善

**预期收益**：
- 为 Stage 2 提供更丰富的结构化输入
- 减少模型自由发挥空间
- 提升判断准确性

---

### ⚠️ 需要谨慎设计的升级项

#### 4. 升级项 3：轻量 Gate（Phase 3）

**目标**：
- 在 Stage 1.8 和 Stage 2 之间增加检查计划编译逻辑
- 前置过滤明显不匹配的规则

**可行性评估**：⭐⭐⭐⭐
- 概念清晰，价值明确
- 但需要仔细设计 Gate 的判断逻辑
- 避免 Gate 本身变成复杂系统

**建议**：
- Gate 应该是纯代码逻辑，不调用 LLM
- 只处理"明显可判"的场景（主体不对、否定明确、条件不满足）
- 不确定的场景仍然送入 Stage 2
- 可以先做 Prototype，验证收益后再全量上线

**预期收益**：
- 减少 skill 轨噪声
- 降低 LLM 调用成本
- 提升整体性能

#### 5. 升级项 4：统一 Override（Phase 4）

**目标**：
- 将 Stage 2.5 从"局部纠偏"升级为"轻量闭包 override"
- 配置化 override 规则

**可行性评估**：⭐⭐⭐⭐
- 当前 Stage 2.5 已有基础
- 但需要设计 override 配置格式
- 需要考虑 override 优先级和冲突处理

**建议**：
- 先梳理当前所有 override 场景
- 设计统一的配置格式（可以是 JSON Schema）
- 支持规则级别的 override 配置
- 保留代码兜底逻辑

**预期收益**：
- 纠偏逻辑更清晰
- 配置化管理更灵活
- 减少 hardcode

---

## 三、与当前项目的契合度分析

### 当前项目优势

方案正确识别了当前项目的优势：
- ✅ 确定性预处理（Stage 0）
- ✅ 混合召回（Stage 1）
- ✅ 双轨路由（Stage 1.8）
- ✅ 深度精判（Stage 2）
- ✅ 反证纠偏（Stage 2.5）
- ✅ span-only 定位（Stage 3）

### 当前项目痛点

方案也准确识别了痛点：
- ❌ 规则卡片过于扁平，缺少结构化信息
- ❌ 模型 prompt 过于宽泛，自由度太大
- ❌ 例外处理分散，缺少统一机制
- ❌ 主体、时态、否定等判断依赖模型，不够稳定

### 升级方案的针对性

方案的 5 个升级项精准对应了这些痛点：

| 痛点 | 对应升级项 | 优先级 |
|------|-----------|--------|
| 规则扁平 | 升级项 1：规则结构化 | P0 |
| Prompt 宽泛 | 升级项 5：Prompt 改造 | P0 |
| 结构化输入不足 | 升级项 2：锚点抽取 | P1 |
| 例外处理分散 | 升级项 4：统一 Override | P2 |
| 规则过滤不足 | 升级项 3：轻量 Gate | P2 |

---

## 四、实施建议

### Phase 1（立即开始，1-2 周）

**核心任务**：
1. 修改 `RuleCard` Schema，新增 6 个结构化字段
2. 改造 Skills 的 system prompt，应用 8 条工作准则
3. 强化 `JudgmentResult` 的 `decision_basis` 字段

**产出**：
- 新版 `schemas.py`
- 新版 `skills.py` 和 `complex_skills.py`
- A/B 测试报告

**风险**：极低

### Phase 2（2-3 周）

**核心任务**：
1. 扩展 Stage 1.5，新增 5 类锚点抽取
2. 补充规则卡片的结构化数据（612 条规则）

**产出**：
- 新版 `stage1_5_fact_extract.py`
- 更新后的 `rule_cards.json`
- 锚点抽取准确率测试报告

**风险**：中等（需要人工标注部分数据）

### Phase 3（3-4 周）

**核心任务**：
1. 设计轻量 Gate 逻辑
2. 在 Stage 1.8 和 Stage 2 之间插入 Gate
3. 验证 Gate 的过滤效果

**产出**：
- 新增 `stage1_9_gate.py`（或集成到 Stage 1.8）
- Gate 过滤效果报告
- 成本节省分析

**风险**：中等（需要平衡召回率和准确率）

### Phase 4（4-6 周）

**核心任务**：
1. 设计 override 配置格式
2. 重构 Stage 2.5，支持配置化 override
3. 迁移现有 override 逻辑到配置

**产出**：
- Override 配置 Schema
- 新版 `stage2_5_refute.py`
- Override 规则库

**风险**：中等（需要保证向后兼容）

---

## 五、关键风险与应对

### 风险 1：规则结构化数据补充工作量大

**风险描述**：
- 612 条规则需要补充 6 个新字段
- 需要人工审核和标注

**应对措施**：
1. 先对高频规则（Top 100）进行标注
2. 使用 LLM 辅助生成初稿，人工审核
3. 分批上线，逐步完善

### 风险 2：Prompt 改造可能影响召回率

**风险描述**：
- 更保守的 prompt 可能导致召回率下降
- 需要平衡准确率和召回率

**应对措施**：
1. 先在测试集上 A/B 测试
2. 监控准确率、召回率、F1 分数
3. 根据业务需求调整保守程度
4. 保留旧版 prompt 作为 fallback

### 风险 3：Gate 逻辑可能过于复杂

**风险描述**：
- Gate 如果设计不当，可能变成"小型 Stage 2"
- 增加系统复杂度

**应对措施**：
1. Gate 只处理"明显可判"的场景
2. 不确定的场景仍然送入 Stage 2
3. Gate 不调用 LLM，纯代码逻辑
4. 定期 review Gate 的过滤效果

---

## 六、补充建议

### 建议 1：增加可观测性

在升级过程中，建议增强系统的可观测性：

1. **Stage 级别的指标**：
   - Stage 1.5 锚点抽取准确率
   - Stage 1.9 Gate 过滤率
   - Stage 2 各 Skill 的调用分布
   - Stage 2.5 各 override 类型的触发率

2. **规则级别的指标**：
   - 每条规则的召回率、准确率
   - 每条规则的误报类型分布
   - 每条规则的 override 触发率

3. **模型行为指标**：
   - `decision_basis` 分布
   - `unsure` 比例
   - `evidence_span_ids` 数量分布

### 建议 2：建立测试集和评测体系

升级过程中需要持续评测：

1. **构建测试集**：
   - 收集 500-1000 条真实文本
   - 人工标注 ground truth
   - 覆盖各类复杂场景

2. **评测指标**：
   - 准确率、召回率、F1
   - 误报率、漏报率
   - 各类场景的细分指标

3. **回归测试**：
   - 每次升级后运行全量测试
   - 对比升级前后的指标变化
   - 识别退化场景

### 建议 3：保留灰度发布能力

建议在系统中增加灰度发布机制：

1. **Feature Flag**：
   - 控制新功能的开关
   - 支持按比例灰度
   - 支持按规则 ID 灰度

2. **双版本并行**：
   - 新旧版本同时运行
   - 对比输出差异
   - 逐步切换流量

3. **快速回滚**：
   - 发现问题时快速回滚
   - 保留旧版本代码和配置

---

## 七、最终结论

### 方案评价

这份升级方案是一份**高质量、可落地**的技术方案：

- ✅ 目标明确，边界清晰
- ✅ 风险可控，渐进式升级
- ✅ 针对性强，解决实际痛点
- ✅ 落地性强，分阶段推进

### 是否建议实施

**强烈建议实施**，理由如下：

1. **收益明显**：预期可降低 20-30% 误报率
2. **风险可控**：不重构主流水线，渐进式升级
3. **成本合理**：主要是开发成本，无需新增基础设施
4. **可持续**：为后续优化打下良好基础

### 实施优先级

建议按以下优先级推进：

**P0（立即开始）**：
- 升级项 1：规则结构化（至少补充 Top 100 规则）
- 升级项 5：Prompt 改造（立即 A/B 测试）

**P1（1-2 个月内）**：
- 升级项 2：锚点抽取（扩展 Stage 1.5）

**P2（2-3 个月内）**：
- 升级项 3：轻量 Gate（需要先验证收益）
- 升级项 4：统一 Override（需要先设计配置格式）

### 关键成功因素

1. **团队对齐**：确保团队理解"让模型更克制"的理念
2. **数据质量**：规则结构化数据的质量决定升级效果
3. **持续评测**：建立测试集和评测体系，持续监控效果
4. **灰度发布**：保留快速回滚能力，降低上线风险

---

## 八、与论文思想的对比

虽然我没有读到原始论文，但从升级方案可以看出，Codex 借鉴了以下核心思想：

1. **结构化规则表示**：从扁平文本到结构化卡片
2. **约束式推理**：限制模型的自由度，提供结构化输入
3. **分层判断**：将复杂判断分解为多个简单判断
4. **可解释性**：强化 `decision_basis`，明确判断依据
5. **保守原则**：证据不足时优先输出 `compliant` 或 `unsure`

这些思想都是当前 LLM 应用的最佳实践，与项目的实际需求高度契合。

---

## 总结

这份升级方案**非常优秀**，建议立即启动 Phase 1 的实施工作。

核心要点：
- ✅ 方案务实、可落地
- ✅ 风险可控、收益明显
- ✅ 分阶段推进、渐进式升级
- ⚠️ 需要建立测试集和评测体系
- ⚠️ 需要保留灰度发布和快速回滚能力

**建议下一步**：
1. 召开技术评审会，对齐团队认知
2. 启动 Phase 1：规则结构化 + Prompt 改造
3. 建立测试集和评测体系
4. 设计灰度发布机制
