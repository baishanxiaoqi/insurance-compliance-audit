# 项目代码审查报告（模型配置拆分 / 审查点覆盖 / 重点审查点）

日期：2026-03-19  
范围：当前主链路代码、`data/rule_cards.json`、`plan/smoke数据构造种子集.xlsx`、最新 `smoke` 评测结果

---

## 一、审查结论

当前版本已经是一版**效果较强的轻量审核系统**，尤其在最新原始 `smoke` 33 条上的主判定指标已经比较好：

- Precision：0.9333
- Recall：0.8750
- F1：0.9032

但是，如果按你这次提出的三个重点来审查，我的结论是：

> **当前方案还不能算“最优”，还存在 3 个明确的 P0/P1 级缺口。**

1. **模型配置仍然是“单全局模型”思路，没有真正拆成按任务配置。**  
2. **当前规则库和 skills 还不能证明“完整覆盖” `smoke数据构造种子集.xlsx` 中的 54 个三级审查点。**  
3. **“绝对化夸大表述”与“金融用语混淆”虽然已具备明显能力，但都还没有达到“知识、规则、skill 三层完全收口”的状态。**

---

## 二、核心发现

### 1. 模型配置没有真正按任务拆开

#### 现状

当前配置层仍然是全局单模型：

- `LLM_API_BASE`
- `LLM_API_KEY`
- `LLM_MODEL`
- `LLM_ENABLE_THINKING`

虽然已经补了 `Stage 1` / `Stage 2` 的超时、token、thinking budget 配置，但**模型身份本身**仍然只有一套。

代码层表现为：

- `src/moderation/config.py` 只定义了全局 `LLM_*`
- `src/moderation/llm_agent.py` 的 `create_model()` 固定读取 `config.LLM_MODEL`、`config.LLM_API_BASE`、`config.LLM_API_KEY`
- `src/moderation/stages/stage2_deep_judge.py` 只是在调用时传入 `max_tokens` / `thinking_budget`，并没有传入“判定专用模型”

#### 更关键的一点

你提出“违规内容使用 1 个模型，批注建议使用另外一个模型”，但当前 `Stage 2.7`：

- **根本没有调用模型**
- 只是模板化 / 规则化生成建议

也就是说，**现在并不存在“建议模型”这条链路**。因此当前代码不是“建议模型没拆开”，而是“建议层尚未进入模型化配置阶段”。

#### 结论

这块需要改，但应该按**任务配置**来拆，而不是继续在全局 `.env` 上堆变量。

更合理的目标结构应当是：

- `filter_model_profile`
- `judge_model_profile`
- `suggestion_model_profile`

每个 profile 独立配置：

- `base_url`
- `api_key`
- `model`
- `thinking_enabled`
- `thinking_budget`
- `timeout`
- `max_retries`
- `max_tokens`

#### 优先级判断

- **优先级：P0**
- 原因：这已经不是调参问题，而是后续模型策略升级的结构性前提。

---

### 2. 审查点覆盖现在还“不可证全覆盖”

#### 我确认到的事实

`plan/smoke数据构造种子集.xlsx` 当前共有 **54 个三级审查点**。

而当前规则库虽然已经有 **551** 条规则，但存在两个关键问题：

#### 问题 A：规则卡片没有审查点主键映射

当前 `data/rule_cards.json` 中：

- `audit_point_id = 0`
- `primary_category = 0`

也就是说，规则库虽然大，但**没有把规则与三级审查点做显式绑定**。

这会导致两个后果：

1. 你很难回答“1.1.1 是否真的被覆盖”
2. 你也很难做稳定的覆盖率评估、回归评估、缺口盘点

#### 问题 B：skill 覆盖仍有明显兜底区

静态路由统计显示：

- `营销合规检测`：225 条
- `信息真实性检测`：133 条
- `通用合规检测`：69 条
- `收益类违规检测`：55 条
- `用语合规检测`：50 条
- `消费者保护检测`：19 条

其中有 **69 条规则仍落入 `DEFAULT_SKILL`（通用合规检测）**。

复杂场景分类统计显示：

- `<none>`：323 条
- `subject_switch`：129 条
- `comparison_or_absolute`：23 条
- `commitment_strength`：21 条
- `gifts_or_extra_benefits`：10 条
- `guaranteed_return`：6 条
- `national_or_regulatory_endorsement`：6 条
- `agent_title_or_recruitment`：7 条
- `tax_or_law_misinterpretation`：7 条
- `transfer_or_inheritance`：1 条

这说明：

- 当前并不是“所有审查点都有明确专属处理路径”
- 相当一部分规则仍处于“泛 skill + 通用 prompt”状态

#### 我的覆盖判断

从“规则存在性”看，系统已经覆盖了不少三级点；但从“规则-审查点-技能”的**闭环映射**看，当前还不能说已经**全部覆盖**。

更准确的说法是：

> 当前是“**大部分点有词面或规则覆盖**，但**不是全部点都有可证明、可稳定回归的专属覆盖**”。

---

### 3. “绝对化夸大表述”与“金融用语混淆”确实是当前最值得优先优化的两类

这点我完全同意，而且从静态结构看，它们的重要性也已经被代码和规则规模体现出来了。

---

## 三、重点专题审查

### A. 金融用语混淆：规则量够大，但 skill 体系还没完全收口

#### 当前优势

这是当前规则库里最强的一簇之一：

- `category_group=financial_confusion`：**172 条**
- 以“理财 / 投资 / 储蓄 / 存款 / 本金 / 利息 / 账户 / 保本 / 杠杆”等为核心字面的直接相关规则：**53 条**

说明当前项目对这类问题已经投入了大量规则资源。

#### 当前问题

但它在 skill 层没有真正形成**单独的专业能力中心**。

目前这类规则会被分散到：

- `收益类违规检测`
- `消费者保护检测`
- `通用合规检测`
- 个别复杂场景 skill

这会带来三个问题：

1. **边界不统一**：有的走收益承诺思路，有的走消费者保护思路，有的走通用判断
2. **例外口径不统一**：例如“保险公司投资”“产品说明书中的机制说明”“否定违规行为的培训文本”等，很容易在不同 skill 下出现口径漂移
3. **类别输出不稳定**：这也是当前 `category_hit_rate` 偏低的重要原因之一

#### 我的结论

> **金融用语混淆在规则层已经够重，但在 skill 层还不够独立。**

如果它是你最重要的审查点之一，那么它应该升级为：

- 独立 `financial_confusion` skill
- 独立 few-shot
- 独立例外分层（非保险主体 / 否定语境 / 机制说明 / 产品对比说明 / 公司投资行为）
- 独立 category 输出约束

#### 优先级判断

- **优先级：P0**

---

### B. 绝对化夸大表述：已有能力，但显式规则覆盖还不够厚

#### 当前优势

这类能力当前已经不是空白：

- `category_group=absolute_expression`：21 条
- `category_group=comparison_violation`：21 条
- `complex_classifier` 已有 `comparison_or_absolute`
- `complex_skills.py` 已有专门的 `SKILL_COMPARISON_OR_ABSOLUTE`

这说明架构上已经给了这类问题专门入口。

#### 当前问题

但从规则显式锚点看，仍然偏薄：

- 明确含有 “第一 / 最好 / 最佳 / 唯一 / 最优 / No.1” 等词面的直接规则命中数量并不大
- 当前分类器大量依赖关键词触发，而不是“绝对化对象 + 语义例外”的结构化判定

这会导致两个风险：

1. **召回有空洞**：例如“最佳选择”“首选方案”“独一无二”“不可替代”“天花板”“顶配”等变体可能不稳
2. **例外不够稳**：例如“主观感受”“文学夸张”“历史/人物评价”“带条件的排名”“产品名称包含极限词”这些合规例外，还没有看到足够强的结构化约束

#### 我的结论

> **绝对化夸大表述当前是“有 skill、有方向，但规则粒度和例外边界还不够厚”的状态。**

如果它是你最重要的审查点之一，这一簇最好做成：

- 明确的“绝对化对象”判断（产品 / 服务 / 代理人 / 公司经营 / 主观感受）
- 明确的“例外触发器”判断（主观评价 / 文学表述 / 客观事实 / 条件排名 / 产品名称）
- 明确的“极限词变体词库”

#### 优先级判断

- **优先级：P0**

---

## 四、按三级审查点体系的覆盖判断

下面是我基于当前规则与 skill 结构给出的覆盖判断。

### 1. 覆盖相对较强

这些点当前已经具备较明确的规则与能力基础：

- `1.1 / 1.2` 金融术语混淆、类比银行理财基金
- `3.1 ~ 3.8` 收益承诺 / 固定收益 / 隐性收益 / 结构化收益话术
- `6.5` 礼品、折扣、抽奖、回扣类利益引诱
- `12.1 ~ 12.4` 身份、职位、招募、收入误导
- `13.1 ~ 13.4` 国家/监管/领导/政策背书

### 2. 覆盖中等，但仍需收口

这些点不是没有能力，而是缺少更稳的专属 skill 或结构化例外：

- `2.1 ~ 2.3` 保障范围夸大、理赔宣传夸大
- `6.2 / 6.3` 同业贬损、片面对比
- `7.1 / 7.2 / 7.3 / 7.5` 绝对化、恐惧促销、压迫式促销、不当用语
- `10.1` 买保险送服务超范围
- `14.1` 宗教诱导宣传

### 3. 当前最弱、最不适合说“已完全覆盖”

这些点我不建议现在就说“已经完整覆盖”：

- `8.2` 互联网渠道噱头宣传
- `9.1 ~ 9.6` 品牌、明星、第三方名片、体育赛事、内容授权等侵权场景
- `10.2` 活动规则违规（解释权、奖品超限）
- `11.1 ~ 11.6` 政治宣传、敏感内容、借热点营销

原因不是说这些点完全没有规则，而是：

- 规则数量偏少或分散
- 没有专门 skill
- 例外/边界知识不够结构化
- 很难从当前结构证明“稳定覆盖”

---

## 五、与最新评测结果的关系

最新原始 `smoke` 33 条结果显示：

- 主判定效果已经不错（F1 = 0.9032）
- 但 `category_hit_rate = 0.1875`
- `location_exact_hit_rate = 0.0`

这和本次结构审查是吻合的：

- **主 verdict 已经比较稳**
- 但“审查点类别”还没有真正结构化收口
- 这也意味着“规则覆盖”和“技能覆盖”仍然更多是**能判**，还不是**能稳定解释成具体审查点**

---

## 六、建议的优化优先级

### P0

1. **把模型配置升级为按任务 profile 配置**
   - filter / judge / suggestion 独立模型配置
   - thinking 也跟随 profile，而不是跟随全局模型

2. **为 54 个三级审查点建立显式映射**
   - 规则卡片补 `audit_point_id`
   - 至少补 `primary_category / secondary_category`
   - 建立“审查点 → 规则 → skill”覆盖矩阵

3. **把“金融用语混淆”升级为独立 skill 簇**
   - 不要再分散在多个基础 skill 中
   - 建立统一例外体系

4. **把“绝对化夸大表述”做成更厚的规则 + 例外簇**
   - 极限词变体补足
   - 合规例外结构化
   - 对象限定结构化

### P1

5. **补齐弱覆盖审查点的专属处理路径**
   - 互联网平台噱头
   - 品牌 / 侵权 / 体育赛事
   - 政治宣传 / 热点事件借势
   - 活动规则违规

6. **如果后续真的要“建议模型分开”**
   - 先保留当前 Stage 2.7 的确定性建议生成作为默认链路
   - 再额外增加一个可选的 LLM suggestion renderer
   - 避免把当前稳定的判定链路和展示链路重新耦合起来

---

## 七、最终结论

如果按你这次最关心的三件事来判断：

### 结论 1：模型配置拆分
**还没有完成，当前仍是全局单模型架构。**

### 结论 2：三级审查点覆盖
**不能说已经全部覆盖；当前更准确的说法是“大部分已有规则覆盖，但没有形成可证明的全覆盖闭环”。**

### 结论 3：绝对化夸大表述 / 金融用语混淆
**这两类已经是系统重点能力，但仍然是下一轮最值得优先加固的 P0 方向。**

---

## 八、我给你的最终判断

> 当前项目已经进入“主判定效果较强，但知识体系与模型配置还没有完全工程化收口”的阶段。  
> 所以下一步最值钱的，不是再继续泛化加规则，而是：  
> **把模型配置拆开、把三级审查点映射补齐、把“金融混淆 / 绝对化夸大”做成真正的核心能力簇。**

