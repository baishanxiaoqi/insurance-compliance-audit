# 轻量升级方案（面向当前项目与模型行为）

日期：2026-03-12

## 一、目标

本方案的目标不是重构系统，也不是引入重型平台，而是在**保留当前主链路**的前提下，做一轮**轻量、低风险、可逐步上线**的升级。

升级目标只有四个：

1. 降低误报；
2. 提高复杂语义场景下的一致性；
3. 让模型判断更可控；
4. 不显著增加系统复杂度和运行成本。

---

## 二、边界约束

本次升级明确 **不做** 以下事情：

- 不重构现有 `Stage 0/1/1.5/1.8/2/2.5/3` 主流水线；
- 不引入完整知识图谱平台；
- 不引入向量数据库、GraphRAG、Airflow、Temporal 一类重型基础设施；
- 不把系统改成“全证据检索 + 大模型总裁决”的重方案；
- 不新增复杂后台系统。

结论很明确：  
**这次升级只做“规则更结构化、模型输入更干净、例外处理更前置、输出更保守”。**

---

## 三、总体思路

当前项目已经具备较好的工程底座：

- 确定性预处理；
- 混合召回；
- 双轨路由；
- 深度精判；
- 反证纠偏；
- span-only 定位。

这次升级不改变这个骨架，只做三件事：

1. **让规则从“扁平卡片”变成“轻量结构化卡片”**；
2. **让模型从“直接看规则文本”升级为“看检查计划”**；
3. **让例外、主体、时态、否定等结构判断尽量在模型前完成，或至少在模型后可闭包纠偏。**

---

## 四、模型需要如何升级

这一部分是最重要的。  
不是升级底层模型型号，而是升级**模型在项目里的工作方式**。

### 1. 模型的职责要收缩

当前模型不应该承担以下职责：

- 自己解释完整监管体系；
- 自己推断隐含规则；
- 自己生成字符坐标；
- 在证据不足时强行给结论；
- 在主体不清、时态不清时做激进判定。

模型升级后的职责应收缩为：

1. 只判断**当前 chunk 在当前规则下是否成立**；
2. 只基于输入给定的结构化事实、规则计划和上下文做判断；
3. 当结构不满足或证据不足时，优先输出 `compliant` 或 `unsure`，而不是强行输出 `violation`；
4. 只输出给定 `span_pool` 中可引用的 `span_id`。

### 2. 模型输入要从“规则卡片”升级为“检查计划”

不要再让模型直接消化大段规则说明。  
更合理的是给模型一份压缩后的检查计划：

- 当前规则 ID；
- 规则类型；
- 违规主张类型；
- 主体要求；
- 必须满足条件；
- 可触发例外；
- 当前 chunk 的锚点摘要；
- 当前 chunk 的事实摘要；
- 可引用的 span_id 列表。

模型只对这份检查计划做判断。  
这样做的价值是：

- 降低 prompt 噪声；
- 降低模型混淆多条件、多例外的概率；
- 降低“自由发挥”空间。

### 3. 模型判定规则要更保守

建议明确告诉模型遵守以下规则：

1. **不得从沉默中推断违规**  
   如果文本没有明确表达某个违规主张，不得补充解释。

2. **主体不对齐时，不得判违规**  
   例如客户经历、第三方表述、历史回顾，不应直接按代理人承诺处理。

3. **时态不对齐时，优先降级**  
   “之前/过去/曾经”与“现在/保证/立即”应区别处理。

4. **例外和否定优先级高于表面词命中**
   例如“不要退保”“不得承诺收益”不应按违规主张输出。

5. **证据不足时输出 `unsure`**
   例如排名、收益率、历史业绩、奖项、市场领先等，如果缺少充分依据，应保守处理。

6. **建议语必须保守**
   只能删、弱化、补充披露，不得虚构产品事实、收益数据、批准表述。

### 4. 模型输出结构要强化三个字段

建议后续让模型输出时重点稳定以下三类信息：

1. `verdict`
2. `evidence_span_ids`
3. `decision_basis`

其中 `decision_basis` 不需要很长，但要明确属于哪一类：

- `explicit_violation`
- `exception_applied`
- `actor_mismatch`
- `time_context`
- `negation_context`
- `insufficient_evidence`

这样后续更容易做 Stage 2.5 纠偏和离线评测。

---

## 五、建议的轻量升级项

## 升级项 1：给规则卡片补少量结构字段

不需要把规则做成图数据库，只需要补少量高价值字段。

建议新增或补齐：

- `actor_scope`：谁说的话才适用这条规则；
- `claim_type`：收益承诺、风险淡化、排名声称、退保引导等；
- `exception_group`：这条规则有哪些例外或排除；
- `evidence_required`：是否必须有数据来源或外部依据；
- `route_hint`：更偏 base 还是 skill；
- `mutual_exclusion_group`：哪些规则不能同时作为主结论。

这些字段的作用不是“为了好看”，而是让：

- Stage 1.8 路由更稳；
- Stage 2 prompt 更干净；
- Stage 2.5 override 更容易做。

## 升级项 2：把 Stage 1.5 升级为“锚点抽取”

当前 Stage 1.5 已经能抽取否定、时间、比较、动作等信号。  
下一步不需要做完整 ER 图，只需要再向前半步。

建议新增的锚点：

- `actor`：代理人 / 客户 / 公司 / 第三方；
- `claim`：收益承诺 / 风险弱化 / 比较优势 / 排名 / 历史业绩；
- `time_scope`：过去 / 当前 / 未来 / 限时；
- `evidence_need`：是否涉及需证明的陈述；
- `tone_strength`：保证 / 可能 / 预期 / 建议。

这一步的目标不是做学术图谱，而是为模型提供更稳定的结构输入。

## 升级项 3：在 Stage 1.8 和 Stage 2 之间增加“轻量 Gate”

不建议新开复杂大阶段，但建议增加一层轻量检查计划编译逻辑。

这个 Gate 要做的事：

1. 判断当前规则是否与当前主体匹配；
2. 判断当前规则是否已被明显例外覆盖；
3. 判断是否需要外部证据；
4. 生成简化后的 rule plan 给模型。

如果 Gate 已经可以确定：

- 主体不对；
- 否定明确；
- 条件不满足；
- 排除项成立；

则可以直接：

- 不送入 skill 轨；
- 或送入 skill 轨但标记为低优先级；
- 或预先标记为 `likely_compliant`。

## 升级项 4：把 Stage 2.5 从“局部纠偏”升级为“轻量闭包 override”

当前 Stage 2.5 已有基础，但还比较局部。

后续建议只做一个轻量版本，不做复杂图遍历：

对于每条高风险规则，允许配置：

- `override_by_negation`
- `override_by_actor_mismatch`
- `override_by_time_context`
- `override_by_exception_group`
- `override_by_missing_condition`

然后让 Stage 2.5 按这些 override 规则统一处理。  
这样比继续堆零散 hardcode 更可控。

## 升级项 5：把 skill 轨 prompt 从“宽泛判断”改成“约束式裁决”

给模型的提示应更像裁判，不像专家顾问。

建议 prompt 风格升级为：

- 只判当前规则；
- 只看当前计划；
- 只允许在给定证据范围内作答；
- 没证据就 `unsure`；
- 有结构反证时优先保守；
- 不输出泛化的监管解释。

目标不是让模型更聪明，而是让模型**更克制、更稳定**。

---

## 六、建议的落地顺序

为了不做重，建议按下面顺序推进：

### Phase 1：模型输入收缩

先做：

- 检查计划；
- 更保守的 judge prompt；
- 明确的 `decision_basis` 分类。

这一阶段性价比最高，风险最低。

### Phase 2：锚点补强

再做：

- actor
- claim
- time_scope
- evidence_need

让 Stage 1.5 对后续判断真正有帮助。

### Phase 3：轻量 Gate

再把主体、例外、条件、排除项前移处理。  
这一步会明显减少 skill 轨噪声。

### Phase 4：统一 override

最后再把 Stage 2.5 的反证逻辑统一成轻量闭包式 override。

---

## 七、不建议现在做的升级

以下升级现在都偏重，不建议当前阶段做：

- 完整知识图谱；
- 图数据库；
- 复杂多跳检索；
- cross-encoder reranker 全量接入；
- 全量外部知识库 RAG；
- 模板平台、后台平台、调度平台；
- 大规模模型微调。

原因很简单：  
这些东西都会显著增加复杂度，但当前项目的第一收益点还不在这里。

当前最该做的是：  
**让规则更结构化，让模型更克制，让判断更可解释。**

---

## 八、建议给模型的升级指令（可直接作为后续提示词原则）

可以把下面这组原则作为后续 judge 模型的统一工作准则：

1. 你只判断当前文本片段在当前规则下是否成立。
2. 你不得扩展规则，不得自行补充监管解释。
3. 你只能使用输入中给出的事实、锚点、规则计划和 span_id。
4. 如果主体不匹配、时态不匹配、存在明确否定、存在明确例外，则优先判定为 `compliant` 或 `unsure`。
5. 如果涉及收益、排名、历史业绩、数据来源等需证明陈述，而输入未提供充分支持，则输出 `unsure`。
6. 如果判定为 `violation`，必须给出最小必要的 `evidence_span_ids`。
7. 如果无法从给定输入中得到稳定结论，不得猜测，不得补全缺失事实。
8. 修改建议只能做删减、弱化、补充披露，不得虚构事实。

这组原则会比“换一个更强模型”更立刻有效。

---

## 九、最终结论

这次升级建议可以概括成一句话：

**不做重型平台，不追求大而全，只把当前系统升级成“规则更结构化、模型更克制、例外处理更稳定”的版本。**

如果继续压缩成更短一句：

**先升级模型的工作方式，再考虑升级系统的体量。**
